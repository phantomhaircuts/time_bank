<h1><%= @profile.name %></h1>
<div class="circular">
<!-- <img src="<%=@profile.pic_url%>"> -->
</div>
<span class="timebank">
Time Bank:
</span>
<span class="timebank">
<%= @profile.time_bank %>:00
</span>
<ul>
Services<%= link_to "+", new_profile_service_path(@profile) %>:
  <ul>
    <% @profile.services.each do |service| %>
      <li>
        <%= link_to "#{service.name}", profile_service_path(@profile, service)%>
      </li>
    <% end %>
  </ul>
